# philrox's Homebrew Tap

Homebrew formulae for philrox CLI tools.

## Install

```bash
brew tap philrox/tap
```

## Install Packages

```bash
brew install philrox/tap/<name>
```

## Packages

### Formulae

- `ris` — Austrian legal information system (RIS) CLI

## Update / Uninstall

```bash
brew update
brew upgrade

brew uninstall <formula>
```

## Notes

- Run `brew info philrox/tap/<name>` for per-tool caveats.
